You are an expert in Automated Planning, Robotics, and PDDL.
I am providing images representing the initial state, an intermediate state, and the goal state of a tabletop robotic manipulation task.

Context: 
The task is performed by a single robotic manipulator operating on a tabletop workspace. The images represent the initial state, an intermediate execution state, and the final state of the task. Your goal is to infer the manipulation task from the visual observations and generate a valid PDDL representation.
For this task: Initially, the mug is on the table and there is a bottle besides it. This is a pouring task. The goal state is to have the robot pick up the bottle pour the liquid in the cup and then put down the bottle at the same place.

Instructions: 
1. Analyze the three images carefully.
2. Identify:
   - all relevant objects
   - object types
   - symbolic locations
   - object states
   - the robot's interactions with the objects
3. Infer the task being performed and the sequence of actions required to transform the initial state into the goal state.
4. Generate a STRIPS-compatible PDDL domain and problem.

Primitive Actions:
Model the task using high-level robotic manipulation primitives rather than low-level motion commands.
Use only actions that correspond to common manipulation skills such as:
- move
- grasp
- place
- pour
- release
- open 
- close 
- insert
- remove
- push
- pull
- wipe
- press
- rotate
Only include the primitives that are actually required for the inferred task. Do not invent unnecessary actions.
Assumptions:
- Use typed PDDL.
- Assume a single robot with one gripper.
- Ignore motion planning, inverse kinematics, collision checking, perception, and grasp synthesis.
- Represent movement symbolically using locations.
- Use only predicates that are required by at least one action.
- Every action should have meaningful preconditions and effects.
- The generated domain should be reusable across similar tasks.
- The generated problem should instantiate the domain for the specific scene shown in the images.
- Assume deterministic execution.
- Model only observable state changes from the images.
- Keep the domain general enough so that other instances of the same task can be represented by modifying only the problem file.
- Every predicate should be used by at least one action.
- Every action should contribute toward achieving the goal.
Output Format:
Provide:
1. A brief description of the inferred task.
2. domain.pddl file
3. problem.pddl file
4. The expected plan generated by a classical planner.
5. A short explanation of the predicates and action schemas.
The generated PDDL should be syntactically correct and compatible with classical planners such as Fast Downward.